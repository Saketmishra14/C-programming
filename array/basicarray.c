#include<stdio.h>
int main()
// {
//     int arr[5]={2,4,6,8,10};// index is the unique identity of array's element.
//     //printf("%d",arr[3]);
//     for (int i = 0; i < 5; i++)
//     {
//      printf("%d ",arr[i]);
//     }
    
//     return 0;
// } 
// //loop always run for the no of index.
{
    int arr[5];
   scanf("%d",arr[1]);
   scanf("%d",arr[2]);
   scanf("%d",arr[3]);
   scanf("%d",arr[4]);
   scanf("%d",arr[5]);
    for (int i = 0; i < 5; i++)
    {
    printf("%d ",arr[i]);    
    }
    return 0;
}