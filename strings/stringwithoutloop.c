#include<stdio.h>
#include<string.h>
int main()
{
    char str[]="My name is Ayush shukla";
    printf("%s\n",str);
    puts(str);
    puts("I will be software Engineer");
    return 0;
}
//%s used for strings.
//puts() used to print the string.
//Put consider statment as a string.